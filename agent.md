# Code Architecture Rules

本项目优先考虑可维护性、清晰的职责边界和可测试性。新增或修改代码必须遵守以下规则。

## File Size

- 普通源代码文件建议保持在 100–300 行。
- 原则上不得超过 400 行。
- 超过 400 行时，应优先按照职责拆分。
- 测试、生成代码、配置、类型声明和静态数据文件可以合理例外。
- 不得为了满足行数限制而机械拆分高度相关的代码。

## Responsibility

- 每个文件只承担一个明确职责。
- 每个函数只完成一个主要任务。
- UI、业务逻辑、数据访问、数据转换和状态管理应当分离。
- 禁止在页面或 UI 组件中堆积复杂业务计算。
- 禁止创建承担多种无关职责的 manager、service、utils、helpers 或 common 文件。

## Dependencies

- 保持清晰的单向依赖。
- 底层模块不得依赖页面、入口或 UI 层。
- 禁止循环依赖。
- 禁止绕过模块公开接口访问其内部实现。
- 公共模块只导出其他模块真正需要的接口。

## Abstraction

- 提取真实存在的重复逻辑。
- 不要为了“看起来架构化”而增加无意义的抽象层。
- 两段代码只是表面相似但业务含义不同，不应强行合并。
- 不创建只有一层无意义转发的 wrapper。
- 工具模块必须使用具体职责命名，禁止万能工具文件。

## Refactoring

进行结构性修改前：

1. 阅读相关模块及调用方。
2. 确认现有行为和接口。
3. 制定拆分方案。
4. 确认依赖方向。
5. 再开始修改。

完成修改后必须运行项目已有的：

- formatter
- lint
- type check
- build
- unit tests
- integration tests

不得通过删除测试、降低类型安全、增加不必要的 `any`、关闭规则或忽略错误来使检查通过。

## Change Scope

- 优先进行小规模、可验证的重构。
- 不在一次修改中混入无关功能。
- 不擅自更换框架、状态管理方案或核心依赖。
- 重构原则上不得改变现有功能、接口、数据格式和用户可见行为。
- 发现超出当前任务范围的问题时，记录问题，但不要顺手大范围改动。

每次新增文件前，先确认现有模块中是否已有合适位置；每次向文件加入较大功能前，先判断该功能是否属于该文件的核心职责。
